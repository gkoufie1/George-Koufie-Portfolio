version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static site - no build step required"
  artifacts:
    baseDirectory: /
    files:
      - index.html
      - robots.txt
      - sitemap.xml
      - assets/**/*
      - resume/**/*
  cache:
    paths: []
